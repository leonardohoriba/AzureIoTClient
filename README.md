# Stingray Daemon (stingrayd)
Socket server running as a daemon when the robot starts.


### Install packages
pip install -r requirements.txt

### .env
Azure IoT Hub device secret keys

SCOPE_ID=
DEVICE_ID=
DEVICE_KEY=

### Python version
Python 3.10
